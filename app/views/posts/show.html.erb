<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @post.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @post.content %>
</p>
<% if logged_in? %>
  <p>
    <strong>User:</strong>
    <%= @post.user.name %>
  </p>
<% end %>

<% if logged_in? %><%= link_to 'Edit', edit_post_path(@post) %> |<% end %>
<%= link_to 'Back', posts_path %>
